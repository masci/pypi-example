import os, sys

os.environ['DJANGO_SETTINGS_MODULE'] = 'pypi.settings'
sys.path.append(os.environ['OPENSHIFT_REPO_DIR'] + '/wsgi/pypi')

import django.core.handlers.wsgi

application = django.core.handlers.wsgi.WSGIHandler()
